import { useFetch } from "../../hooks/useFetch";

import {useFetch} from './../../hooks/useFetch'

const Episodes = () => {
    const [data, fetching , error] = useFetch("episodes")
      return (  );
}
 
export default Episodes;